/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "www.loopple.com",
      "images.unsplash.com",
      "demos.creative-tim.com",
    ],
  },
}

module.exports = nextConfig
